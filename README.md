Mirror of Google Web Fonts: http://code.google.com/p/googlefontdirectory/source/checkout

Contains only latest TTF files for easy installation

find /path/to/hg/repo -maxdepth 3 -type f -name \*.ttf -exec cp {} ~/.fonts \;
